// import {
//   renderSortedItems,
//   onChangeSortSelect,
// } from "../../sort/sort/sort.mjs";
export const sortBtn = (sortOption, listings) => {
  // const select = document.querySelector("select#sort");
  // select.addEventListener("change", (event) =>
  //   // renderSortedItems(sortOption, listings)
  //   onChangeSortSelect(event, listings)
  // );
};
